# Stickman AdBlock (Chromium Extension)

A production-oriented Manifest V3 Chromium extension inspired by uBlock Origin design principles, focused on:

- Blocking YouTube/video and website ad requests.
- Blocking common tracking/analytics endpoints.
- Blocking risky and known malicious ad domains.
- Reducing nuisance UX such as cookie walls, floating videos, and newsletter popups.
- Parsing EasyList/uBO-compatible filter syntax subset for dynamic updates.

## uBO-Inspired Basis

This project is based on the architecture and filtering approach used by uBlock Origin:

- Multiple independent protection layers (ads, trackers, malware, cosmetic).
- Filter-list driven blocking strategy.
- Periodic list refresh and user-triggered manual refresh.
- Last-known-good fallback to keep protection active during list source outages.

This project does **not** copy uBlock Origin source code. It implements an original codebase that follows similar concepts and uses publicly available filter lists.

## Project Structure

- `manifest.json`: Extension manifest and capability declarations.
- `src/background/service-worker.js`: Protection state management and DNR ruleset control.
- `src/content/page-cleaner.js`: Cosmetic filtering and nuisance overlay cleanup.
- `rules/core-ads.json`: Core ad network and ad URL pattern rules.
- `rules/tracking.json`: Tracker and telemetry blocking rules.
- `rules/malware.json`: Malicious/risky ad domain blocking rules.
- `popup/`: Popup UI for runtime toggles.
- `tools/validate.js`: Local validation script for manifest/rules/basic JS syntax.
- `docs/ARCHITECTURE.md`: Design and safety notes.
- `docs/REFERENCES.md`: Upstream inspirations and filter-list attributions.

### Clean Workspace Notes

- `_metadata/` is generated by Chromium for unpacked extensions and is not part of source.
- Keep source of truth in `manifest.json`, `src/`, `rules/`, `popup/`, and `docs/`.

## Load In Chrome (Developer Mode)

1. Open `chrome://extensions`.
2. Enable **Developer mode**.
3. Select **Load unpacked**.
4. Choose this folder: `adblocker-project`.

## Usage

1. Click the extension icon.
2. Toggle protection layers:
   - Enable protection
   - Block ad networks
   - Block trackers
   - Block risky ad domains
   - Remove cookie walls
   - Hide distractions
   - Aggressive cleanup mode
   - Use remote filter lists (uBO-style)
3. Optionally click **Update filter lists now** for immediate list refresh.

The badge shows `OFF` when disabled. When enabled, no badge number is shown.

## Remote List Sources

- EasyList
- EasyPrivacy
- uBlock Origin uAssets filters

The parser supports a practical subset of EasyList/uBO static syntax suitable for MV3 DNR conversion.

Supported option subset currently includes:

- Resource type options (`script`, `image`, `xhr`, `stylesheet`, etc.)
- `domain=` include list constraints
- `third-party`

## Validation

If you have Node.js installed:

```bash
node tools/validate.js
```

This checks:

- `manifest.json` format and required fields.
- DNR rules file format and duplicate rule IDs.
- JavaScript syntax via `node --check`.

## Important Notes

- This extension is designed for personal use and iterative improvement.
- Ad ecosystems evolve rapidly; update rules over time.
- Aggressive cleanup can occasionally hide legitimate overlays on some sites. Keep it off unless needed.
- MV3 has limits compared to the full uBO engine, so not all advanced filter syntax can be represented.

## Hardening Ideas (Next)

- Add domain-level allowlist/denylist UI.
- Add optional remote list updater with signed list integrity checks.
- Add e2e browser automation tests for major target sites.
- Add per-site quick disable shortcut from popup.
